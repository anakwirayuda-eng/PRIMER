---
description: Onboarding for AI agents — read context before working on PRIMER
---

# AI Agent Onboarding

Before making ANY changes to the PRIMER codebase:

// turbo
1. Read `PRIMER_BIBLE.md` in root (game identity, purpose, values, tech stack)
// turbo
2. Read `docs/ARCHITECTURE_LOG.md` (architecture decisions, store shape, ACL, patient pipeline)
// turbo
3. Check `git log -5 --oneline` for recent changes
// turbo
4. Read `docs/BUG_TRIAGE_LOG.md` if working on bug fixes (avoid repeating old mistakes)
